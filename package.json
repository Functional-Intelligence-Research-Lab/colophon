{
  "name": "colophon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser extension that records AI writing interactions as a TWFF process log.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run test && npm run build && echo '\\n✓ All checks passed.'",
    "status": "npm run build --silent && node tools/status.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.25.0",
    "eslint": "^9.0.0",
    "vitest": "^4.1.5"
  }
}
